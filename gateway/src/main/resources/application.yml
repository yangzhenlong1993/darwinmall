spring:
  cloud:
    gateway:
      routes:
        - id: google_route
          uri: https://www.google.com
          predicates:
            - Query=url, google
        - id: qq_route
          uri: https://www.qq.com
          predicates:
            - Query=url, qq